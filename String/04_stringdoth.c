#include<stdio.h>
#include<string.h>
int main()
{
    // char str[]="Alok Yadav";

    // // 1. strlen
    // int length=strlen(str);   // it does not count \0
    // printf("\nThe length is %d\n",length);

    // // 2. strcpy
    // char str2[40];
    // strcpy(str2,str);
    // puts(str2);

    // // 3. strcat
    // char str3[]="The ";
    // strcat(str3,str);
    // puts(str3);

    // 4. strcmp
    int result=strcmp("Jerry","Jerry boy");
    printf("\nThe result is %d",result);

}